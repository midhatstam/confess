name: Delete merged branch

on: 
  pull_request:
    types: [closed]

jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - name: Delete
      - uses: SvanBoxel/delete-merged-branch@v1.3.3
        if: github.branch.name != 'develop'
      - run: delete-merged-branch
